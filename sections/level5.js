function initLevel5() {
  const container = document.getElementById('level5-content');
  container.innerHTML = `
    <div style="font-family:var(--font-mono); color:var(--chalk-dim); font-size:14px; padding:24px; border:1px dashed var(--border); border-radius:var(--radius); text-align:center;">
      LEVEL 5 — COMING SOON. SECTION UNLOCKED SUCCESSFULLY.
    </div>
  `;
}
